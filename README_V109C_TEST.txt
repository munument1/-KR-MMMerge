MMMerge Korean v1.0.9c quick-save test update

Changes:
- Quick saves keep GrayFace's Quick Save1, Quick Save2, ... titles instead of the current region name.
- Quick-save rotation count is set to 10 by Data/KO_QuickSave.ini.
- Quick-save name is set to Quick Save with no extra space before the number.
- mm8.ini is backed up once as mm8.ini.ko-quicksave-backup before these three options are updated.

The live count is applied when supported. Restart the game once after installation so all mm8.ini string settings are guaranteed to take effect.
Completely exit the game before overwriting files.
