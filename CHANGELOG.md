# 변경 기록

## v1.0.6

- 기존 세이브에 복사되어 번역 테이블 갱신 후에도 영문으로 남던 NPC 이름 수정
- NPC 이름 원본 표를 읽을 때 영문→한국어 대응을 보존하고 맵 로드 후 `Game.NPC`에 적용
- `vars.RndNPCPersist`에 영구 저장된 무작위 NPC 이름도 변환해 재저장 후 영문으로 되돌아오는 문제 방지
- `Margaret the Docent`가 기존 세이브에서도 `도슨트 마거릿`으로 표시되는 것을 실제 게임에서 확인
- 고정 NPC 이름 1,148개와 무작위 NPC 이름 840개의 대응표 충돌 검사 완료

## v1.0.5

- MM6에서 MM8의 `파멸자의 날` 기록이 표시되던 대륙별 History 테이블 잔류 문제 수정
- MM8·MM7 기록 파일을 대륙에 맞게 다시 불러오고 MM6에는 전용 `엔로스 연대기` 소개 추가
- 구버전의 혼합 `KO_HistoryTxt.txt`가 남아 있어도 자동으로 무시하도록 현지화 로더 수정
- 현지화 폴더에서 `KO_*.txt` 파일만 읽도록 범위를 제한해 덤프·백업 파일의 오적용 방지
- 오류를 일으켰던 폐기 파일 `History.lua`를 되살리지 않고 `KoreanHistory.lua`로 안전하게 구현
- MM7·MM8 History 본문을 한국어로 분리하고 MM8 첫 기록은 8KB 버퍼를 넘지 않는 검증된 번역으로 복구
- 마차회사 이동 문구 3곳의 `%d`·`%s` 순서를 원문과 맞춰 진입 시 접근 위반이 발생하던 문제 수정
- 2026-07-28 09:14 실제 게임 백업에서 깨끗한 14·15b·16픽셀 DBCS 글꼴을 복원
- 서적의 29픽셀 큰 제목만 충주김생체로 적용
- 기존 글꼴에 없던 `썽`을 피하도록 `말썽을 일으키지 마시오`를 `문제를 일으키지 마시오`로 교정

## v1.0.4

- 작은 UI 글씨(14·15·16픽셀)를 프리텐다드 SemiBold로 교체(이후 v1.0.5에서 검증된 이전 게임 백업으로 복구)
- 작은 UI 글씨와 보존 문장부호의 기존 그림자 픽셀 제거
- 서적 및 큰 글씨(29픽셀)를 충주김생체로 교체
- EUC-KR 완성형 한글 글리프 전체 생성
- DBCS 폰트 104개 구조·높이·런타임 로딩 검증
- 폰트 출처, 원본 해시 및 라이선스 고지 추가
- 손상된 저장 슬롯 제목을 맵 원문 이름으로 복구하고 원본 저장파일 백업 생성
- 저장 후 새로 손상된 슬롯 제목도 자동 복구하도록 런타임 처리 추가
- `New Sorpigal` 표지판 철자 오류와 지도 스크립트 적용 순서 수정
- `evt.str`뿐 아니라 이미 복사된 `evt.hint`의 영문 지명도 현지화

## v1.0.3

- 누락됐던 `LocalizeSignposts.lua` 배포 포함
- 일반 NPC 직업 77개 번역 및 런타임 로더 추가
- NPC 표시 형식 `%s the %s`를 `%s - %s`로 변경
- `GlobalTxt` 시스템 문자열 758개 보완
- `morning/day/evening`을 `아침/하루/저녁`으로 번역해 `좋은 day입니다!` 혼합 문구 수정
- 시스템 문자열의 printf 서식 토큰 보존 검증

## v1.0.2

- 사용자가 올린 `KO_NPCText`, `KO_NPCGreet1`, `KO_NPCGreet2`를 재가공하지 않은 원본으로 복원
- `unfinished string near` 오류를 일으킨 폐기 파일 `Scripts/General/History.lua` 제거
- NPC 이름·주문 번역 및 현지화 로더 수정은 유지
- 복원된 원본 번역 파일로 게임 런타임 재검증

## v1.0.1 (철회됨)

- NPC 대화 및 인사말 번역 누락 수정
- 무작위 NPC 이름 남성 539개·여성 310개 번역 추가
- 주문 132종의 이름·설명·숙련도 효과 번역 추가
- 여러 줄 NPC 대화를 한 레코드로 온전히 불러오도록 로더 수정
- 한국어 인코더가 `gsub`의 치환 횟수를 함께 반환해 NPC 이름 로딩이 중단되던 문제 수정
- Merge 내부에서 참조하는 NPC 이름 배열을 제자리에서 교체하도록 수정
- 기본 텍스트 로더가 끝난 뒤 한국어 현지화를 적용하도록 이벤트 순서 수정

## v1.0

- 최초 한국어 패치 공개
